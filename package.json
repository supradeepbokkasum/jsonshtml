{
  "name": "json-tool",
  "version": "0.0.1",
  "main": "functions/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy ./dist --branch main"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^4.0.0"
  }
}

